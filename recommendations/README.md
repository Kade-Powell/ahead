# Recommendations

This folder catalogs optional third-party skills reviewed for AHEAD workflows.

Every recommendation must pin the exact reviewed revision, license, source, applicable phases, and compatibility limits. Recommendations are never bundled or installed automatically, and AHEAD policy remains authoritative.

Update the versioned catalog only after reviewing the new source. Validate changes with `npm test` from `integrations/pi`.
